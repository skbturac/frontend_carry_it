import React, { Component } from 'react';

class Packages extends Component {
  render() {
    return (
      <div className="package-container">
        "packages listed as cards"
      </div>
    );
  }
}

export default Packages;
