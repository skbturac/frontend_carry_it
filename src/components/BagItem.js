import React, { Component } from 'react';

class BagItem extends Component {
  render() {
    return (
      <div className="bag-item-container">
        "Inf of each BagItem displayed in each card"
      </div>
    );
  }
}

export default BagItem;
